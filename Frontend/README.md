# Research Hub Frontend

## Setup

1. Install dependencies:

```bash
npm install
```

2. Start the development server:

```bash
npm run dev
```

## Environment

Set the API base URL if your backend is running on a different port:

```bash
VITE_API_URL=http://localhost:5000
```
